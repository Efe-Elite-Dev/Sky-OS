# Gerekli klasör yapısını oluşturuyoruz
mkdir -p iso/boot/grub

# Aşağıdaki komut direkt olarak grub.cfg dosyasını içeriğiyle birlikte oluşturur
cat << 'EOF' > iso/boot/grub/grub.cfg
set timeout=5
set default=0

menuentry "Sky-OS Saf AI Kernel" {
    multiboot /boot/mykernel.bin
    boot
}
EOF
