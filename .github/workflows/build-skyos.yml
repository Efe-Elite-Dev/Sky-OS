name: Sky-OS Auto Build CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-os:
    runs-on: ubuntu-latest

    steps:
    - name: Depoyu Klonla (Checkout)
      uses: actions/checkout@v4

    - name: Gerekli Derleme Araçlarını Kur
      run: |
        sudo apt-get update
        sudo apt-get install -y nasm gcc-multilib xorriso grub-pc-bin mtools

    - name: Kaynak Kodları Derle
      run: |
        echo "[+] boot.asm derleniyor..."
        nasm -f elf32 boot.asm -o boot.o
        
        echo "[+] kernel.c derleniyor..."
        gcc -m32 -c kernel.c -o kernel.o -std=gnu99 -ffreestanding -O2 -Wall -Wextra
        
        echo "[+] Linker Script ile birleştiriliyor..."
        ld -m elf_i386 -T linker.ld boot.o kernel.o -o mykernel.bin

    - name: ISO Klasörlerini ve GRUB Yapılandırmasını Otomatik Oluştur
      run: |
        # Klasör yapısını GitHub sunucusunda dinamik olarak kuruyoruz
        mkdir -p iso/boot/grub
        
        # Derlenen kerneli olması gereken yere taşıyoruz
        cp mykernel.bin iso/boot/
        
        # grub.cfg dosyasını sıfırdan tam yerine yazıyoruz
        cat << 'GRUB_EOF' > iso/boot/grub/grub.cfg
        set timeout=5
        set default=0
        menuentry "Sky-OS Saf AI Kernel" {
            multiboot /boot/mykernel.bin
            boot
        }
        GRUB_EOF

    - name: Boot Edilebilir ISO İmajını Üret
      run: |
        grub-mkisofs -o skyos.iso iso

    - name: ISO Dosyasını GitHub'a Yükle (Artifact)
      uses: actions/upload-artifact@v4
      with:
        name: Sky-OS-Sanal-Disk
        path: skyos.iso
        retention-days: 7
